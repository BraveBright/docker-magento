version: "3"
services:
  db:
    image: mysql:5.7
    container_name: m22-db
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: 'no'
      MYSQL_ROOT_PASSWORD: pass1234
      MYSQL_DATABASE: magento2
    volumes:
      - m22-db:/var/lib/mysql
    networks:
      magento_net:
        ipv4_address: 192.168.101.10
    logging:
      driver: json-file
  web:
    build: ./apache2/php7.1
    image: magento:7.1-apache
    container_name: m22-web
    links:
      - db
    volumes:
      - ./apache2/magento.conf:/etc/apache2/sites-enabled/magento.conf
      - ./magento:/var/www/magento:cached
      - ./apache2/php7.1/php.ini:/usr/local/etc/php/php.ini
      - m22-composer:/root/.composer
    depends_on:
      - db
    networks:
      magento_net:
        ipv4_address: 192.168.101.20
    logging:
      driver: json-file
volumes:
  m22-db:
  m22-composer:
networks:
  magento_net:
    ipam:
      config:
        - subnet: 192.168.101.1/24
